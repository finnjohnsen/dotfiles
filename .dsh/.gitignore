# Secrets — never commit
.credentials.yaml
.env
.env.*
!.env.example

# Machine-local runtime state
sessions/
storages/
attachments/

# Installed dependencies (symlinks, regenerated by pnpm)
profiles/node_modules/

# Snapshot replay configs (machine-local)
cordis.snapshot.yml
*.snapshot.yml
*.snapshot.yaml
